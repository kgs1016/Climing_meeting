# MVP v1 설치 가이드 (약 10분)

참가자 앱과 운영 콘솔을 돌리려면 Supabase 프로젝트 하나가 필요합니다. 무료 플랜으로 충분해요.

---

## 1. Supabase 프로젝트 만들기

1. [supabase.com](https://supabase.com) 가입 → **New project**
2. 이름은 아무거나 (예: `hobiday`), **Region은 Northeast Asia (Seoul)** 권장
3. Database Password는 잘 저장해두세요 (나중에 쓸 일은 거의 없음)
4. 생성까지 1~2분 대기

## 2. 스키마 실행

1. 왼쪽 메뉴 **SQL Editor** → **New query**
2. [`supabase/schema.sql`](supabase/schema.sql) 내용을 **전부 복사해서 붙여넣기**
3. 맨 아래 줄의 운영자 키를 **반드시 바꾸기**:

```sql
insert into app_config(key, value) values ('admin_key', 'CHANGE_ME_긴_랜덤_문자열')
```

`CHANGE_ME_긴_랜덤_문자열` 자리에 추측 불가능한 긴 문자열을 넣으세요.
이게 운영 콘솔 비밀번호 역할을 합니다. **이 값은 코드나 깃에 절대 넣지 마세요.**

4. **Run** 클릭 → `Success` 확인

## 3. 연결 정보 넣기

1. 왼쪽 메뉴 **Project Settings → API**
2. 아래 두 값을 복사해 [`config.js`](config.js)에 채웁니다:

| Supabase 화면 | config.js |
|---|---|
| Project URL | `url` |
| Project API keys → `anon` `public` | `anonKey` |

```js
window.HOBIDAY_CONFIG = {
  url:     'https://abcdefgh.supabase.co',
  anonKey: 'eyJhbGciOi...'
};
```

3. 커밋 & 푸시 → GitHub Pages에 자동 반영

> **anon 키는 공개돼도 괜찮습니다.** 모든 테이블에 RLS가 켜져 있고 정책이 없어서 직접 접근이 차단되며,
> 데이터는 `SECURITY DEFINER` 함수로만 오갑니다. 반면 **admin_key는 절대 공개하면 안 됩니다.**

---

## 사용 흐름

### 운영자 — [console.html](console.html)

1. 접속 → **운영자 키**(admin_key) 입력 (브라우저에 저장됨)
2. **새 이벤트** 만들기 (제목·날짜·장소)
3. **참가자 6명 등록** (남3·여3). 이름·나이대·실력·홈짐·연락처 입력 후 `모두 저장/추가`
   - 등록하면 각자의 **개인 링크**가 발급됩니다 → `복사` 버튼으로 복사해 카톡으로 개별 발송
   - ⚠️ 링크 하나에 한 사람만 대응합니다. 섞이지 않게 주의하세요
4. 참가자들이 설문을 채우면 `설문` 열이 **완료**로 바뀝니다
5. `카드 미리보기`로 어떤 카드가 나갈지 미리 확인 가능

### 당일 — 진행 단계 버튼

| 시점 | 누를 버튼 |
|---|---|
| 행사 전 | `카드 비공개` (기본값) |
| R1 그룹 워밍업 후 휴식 | `라운드 2까지 공개` |
| R2 후 휴식 | `라운드 3까지 공개` |
| R3 후 휴식 | `라운드 4까지 공개` |
| 저녁 해산 후 | `최종선택 열림` |
| 전원 제출 확인 후 | `결과 공개` |

참가자는 자기 앱을 새로고침하면 해당 라운드 카드가 나타납니다.

### 참가자 — [app.html](app.html)`?t=개인토큰`

로그인 없음. 받은 링크만 열면 됩니다.

1. **설문** — 취향·클라이밍 스타일 15문항 (3분)
2. **카드** — 라운드가 열릴 때마다 상대의 공통점 카드 표시
3. **선택** — 다시 만나고 싶은 사람 비공개 선택
4. **결과** — **상호 선택된 경우에만** 상대 이름과 연락처 공개

---

## 설계상 지켜지는 것들

- **짝사랑은 절대 노출되지 않습니다.** `my_result` 함수가 상호 선택된 쌍만 반환하고, 매칭이 없으면 "이번엔 연결된 인연이 없었어요"로 끝냅니다. 누가 나를 선택하지 않았는지 알 방법이 없습니다
- **연락처는 상호 매칭 전까지 어디에도 내려가지 않습니다.** 카드 API(`my_rounds`)는 이름·실력·설문만 반환합니다
- **라운드 잠금은 서버에서 강제됩니다.** 아직 열리지 않은 라운드의 상대 정보는 API 자체가 내려주지 않아서, 참가자가 브라우저를 조작해도 미리 볼 수 없습니다
- **운영자 함수는 admin_key를 서버에서 검증**합니다

---

## 자주 막히는 곳

| 증상 | 원인·해결 |
|---|---|
| "설정이 필요해요" | `config.js`에 `YOUR_` 자리표시자가 남아 있음 |
| 콘솔에서 "운영자 키가 틀렸어요" | schema.sql에서 바꾼 `admin_key`와 다르게 입력함 |
| "링크를 확인해주세요" | 토큰이 잘못됐거나 참가자가 삭제됨. 콘솔에서 링크 다시 복사 |
| 카드가 안 보임 | 진행 단계가 `카드 비공개`거나, 상대가 아직 설문 미작성 |
| 결과가 안 보임 | `결과 공개`를 아직 안 켰음 |

---

## 아직 없는 것 (MVP v2 예정)

- 보증금 결제 (PG 연동 — 사업자등록 선행 필요)
- 푸시 알림 (지금은 참가자가 새로고침해야 함)
- 크레딧 적립·사용
- 자동 그룹 편성 (지금은 운영자가 수동으로 6명 배정)
